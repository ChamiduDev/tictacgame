{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['gridSize', 'board', 'currentTurn', 'players', 'status'])"
      }
    }
  }
}
